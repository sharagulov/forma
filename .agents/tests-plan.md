# Tests plan — FORMA

## Рекомендуемый стек

- **Vitest** + **Testing Library** — утилиты `src/lib/utils.ts`, валидация контактов (если вынести чистые функции из route).
- **Playwright** — smoke: `/`, `/projects`, один `/projects/[slug]`, `/about`, `/contact`, отправка формы (mock API или dev).

## План сценариев (не реализовано в репозитории)

| ID | Сценарий | Ожидание |
|----|----------|----------|
| T1 | Главная рендерится, есть hero и навигация | 200, ключевые роли landmark |
| T2 | Фильтр проектов меняет список | DOM обновляется |
| T3 | POST /api/contact с валидным телом (mock Resend) | 200 `{ ok: true }` |
| T4 | POST /api/contact невалидное тело | 400 |

## Команда (после добавления зависимостей)

```bash
npx playwright test
npx vitest run
```

## Статус

План зафиксирован; автотесты в проект не добавлялись в этом проходе.
